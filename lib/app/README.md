# App Layer

The `app/` directory contains the root configurations of the application.

## Responsibilities
- App-wide entry point and configuration.
- Global providers or wrappers.
- High-level app state management initialization.
