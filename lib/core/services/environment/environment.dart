/// [Environment] defines the possible runtime environments for HabitFlow.
enum Environment {
  /// Local development environment.
  development,
  
  /// Staging/testing environment.
  staging,
  
  /// Production environment for end-users.
  production,
}
