import 'package:flutter/material.dart';

/// [HFColors] defines the color palette for the HabitFlow application.
/// 
/// These colors are based on the HabitFlow Design System (HFDS) v1.0.
class HFColors {
  // Brand Colors
  static const Color primary = Color(0xFF10B981); // Emerald Green
  static const Color secondary = Color(0xFF14B8A6); // Teal
  static const Color accent = Color(0xFFF59E0B); // Amber

  // Functional Colors
  static const Color success = Color(0xFF22C55E); // Green
  static const Color warning = Color(0xFFF97316); // Orange
  static const Color error = Color(0xFFEF4444); // Red
  static const Color info = Color(0xFF3B82F6); // Blue

  // Neutral Colors
  static const Color background = Color(0xFFFAFAF9); // Warm White
  static const Color surface = Color(0xFFFFFFFF); // White
  static const Color outline = Color(0xFFE5E7EB); // Light Gray
  
  // Text Colors
  static const Color textPrimary = Color(0xFF1C1C1E);
  static const Color textSecondary = Color(0xFF636366);
  static const Color textDisabled = Color(0xFF9CA3AF);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Theme Specific
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);
}
