# Shared Layer

The `shared/` directory contains code that is shared across multiple features but is not "core" infrastructure.

## Structure
- `components/`: Complex UI blocks composed of multiple widgets.
- `widgets/`: Small, reusable atomic UI elements (buttons, inputs, etc.).
- `models/`: Common data models used by multiple features.
